let i = 1; //Iniciando el while

while(i < 100 ) { //Condicion
    if (i % 2 === 0) {
        console.log(`El numero ${i} es par`);
    } else {
        console.log(`El numero ${i} es impar`);
    }



    i++//Incremento
}